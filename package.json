{
  "name": "ominisounds",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OMINSOUNDS — marketplace de beats da Smooth Produções, parte técnica pela SoftWave.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:migrar": "node --env-file=.env.local scripts/migrar.mjs",
    "db:migrar:pagamentos": "node --env-file=.env.local scripts/migrar-pagamentos.mjs",
    "db:semear": "node --env-file=.env.local scripts/semear.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
